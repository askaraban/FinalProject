<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="swift.air.mapper.ReservationMapper">
	<!-- 출발지, 도착지, 가는여정 출발일, 돌아오는 여정 출발일, 가용 자리 받아와야함 -->
	<!-- route_departure, route_destination, schedule_departure, schedule_departure, seat_id -->
	<select id="selectFlightAvailable" resultType="map">
		select route_departure, route_destination, route_time, schedule_departure_date, scheduel_arrival_date,
			
			
	</select>
</mapper>